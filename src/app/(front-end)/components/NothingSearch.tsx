import { Typography } from "@mui/material";

export default function NothingSearch() {
  return (
    <div className="flex items-center justify-center text-gray-400">
      <Typography variant="h5">검색결과가 존재하지 않습니다.</Typography>
    </div>
  );
}
