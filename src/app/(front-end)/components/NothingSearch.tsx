import { Typography } from "@mui/material";

export default function NothingSearch() {
  return (
    <div className="flex items-center justify-center">
      <Typography variant="h5">
        TODO (짤 넣기)
        <br />
        검색결과가 존재하지 않습니다.
      </Typography>
    </div>
  );
}
